package config

const (
	// UsernameGuest define the name that is guest user
	UsernameGuest = "guest"

	// UserAuditEnableKey define the key-string that determine if write audit log to db.
	UserAuditEnableKey = "enable_user_audit"
)
